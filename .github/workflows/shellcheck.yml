name: shellcheck-action-test
on: [push]

jobs:
  shellcheck:
    name: Shellcheck
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: install shellcheck
      run: sudo apt-get install -y shellcheck
    - name: run shell-check
      run: shellcheck *.sh -e SC1090